# uploads/ holds only optional local artifacts.
# File Explorer trees are stored in SQLite (worker_file_tree), synced via /api/sync-file-tree.
# Legacy {worker}_tree.json files are deleted on startup / worker rename when obsolete or orphaned.
